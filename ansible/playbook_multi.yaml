# playbook_multi.yaml
---
- hosts: all
  vars:
      STATE1: stopped
      ntp_server: us.pool.ntp.org
  tasks:
      - include: playbook_tasks.yaml
        STATE: "{{STATE1}}"

  handlers:
      - include: playbook_handlers.yaml STATE=restarted
